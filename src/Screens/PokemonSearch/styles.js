import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mainContainer: {
        /* borderWidth: 1, */
        marginHorizontal: 20,
        marginVertical: 10
    },
    topContainer: {
        flexDirection: "row"
    }
})

export default styles;